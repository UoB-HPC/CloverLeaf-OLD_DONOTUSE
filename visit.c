#include "visit.h"
#include "definitions_c.h"
#include <string.h>

void visit()
{
    // int j, k, c, err, get_unit, u, dummy, tile;
    // int nxc, nyc, nxv, nyv, nblocks;
    // double temp_var;

    // char name[80];
    // char chunk_name[10], tile_name[10], step_name[10];
    // char filename[90];

    // // FILE *

    // int fields[NUM_FIELDS];

    // double kernel_time;
    // bool first_call = true;

    // strcpy(name, "clover");

    // if (first_call) {
    //     nblocks = number_of_chunks * tiles_per_chunk;
    //     strcpy(filename, "clover.visit");
    //     // fopen
    // }
    // // TODO
}
